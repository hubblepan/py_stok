{
  "compilerOptions": {
    // 用于解析非相对模块名称的基目录。
    "baseUrl": ".",
    // 指定相对于baseUrl选项计算的路径映射。
    "paths": {
      "@/*": ["src/*"]
    },
    // 指定要使用的默认库（lib.d.ts）。 值是“es3”，“es5”，“es6”，“es2015”，“es2016”，“es2017”，“es2018”，“esnext”。
    "target": "es2017",
    // 允许从没有默认导出的模块进行默认导入。 这不会影响代码发出，只会影响类型检查。
    "allowSyntheticDefaultImports": false
  },
  // 如果不存在include属性，则默认为包含目录和子目录中的所有文件
  "include": ["src/**/*", "mock"],
  // 排除的目录列表
  "exclude": ["node_modules", "dist"]
}
